#!/usr/bin/env python

import os
import sys
from subprocess import Popen, PIPE

def send(s):
	p = Popen([ "y9p", "write", "prop" ], shell=False, stdin=PIPE)
	p.communicate(s)

send("tab=" + sys.argv[1])
